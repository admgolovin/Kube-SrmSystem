node {
  checkout scm
  sh "docker build -t admgolovin/jenkins:new ."
}
  stage('Push image') {
    docker.withRegistry('admgolovin/myjenkins', 'dockerhub') {
        app.push("${env.BUILD_NUMBER}")
        app.push("latest")
        }
    }
