node {
  checkout scm
  sh "docker build -t admgolovin/jenkins:new ."
}
  steps {
      withDockerRegistry([ credentialsId: "dockerhub", url: "" ]) {
        sh 'docker push admgolovin/myjenkins:StarkImage'
        }
    }  
